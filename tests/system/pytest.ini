[pytest]
pythonpath = . framework
addopts = --strict-markers -m "not privileged"
testpaths = tests
ticket_tools = bz,gh,jira

markers =
    privileged: tests requiring elevated privileges (mounts, loop devices, BTRFS, etc.)
